# developer 상태 파일

## 현재 상태
1단계(Rapfi 빌드) 완료 + GUI 연동 완료. Piskvork(렌주 지원판, dhbloo/piskvork_renju) GUI를 구해 pbrain-rapfi.exe와 연결, 레지스트리 사전설정(15x15/렌주룰/엔진경로)까지 마쳐 사용자가 더블클릭 한 번으로 바로 대국 가능한 상태. pipe 로그로 `INFO rule 4` 전송 + 렌주 전용 NNUE 가중치 로드 + 실제 수 응답(깊이 18~38)까지 실전 프로토콜 레벨 검증 완료. 남은 것은 사용자 본인의 마우스 대국(3-3/4-4/장목 금수 클릭 테스트)과 최종 xcopy 패키징.

## 완료한 작업 이력
| 날짜 | 작업 | 핵심 결정 |
|------|------|-----------|
| 2026-07-10 | 오프라인 오목 AI 기술 방식 검토 (코드 작성 없음) | "모든 경우의 수 저장" 불필요함을 확인. 프리스타일 오목의 선手 필승은 threat-space search(VCT/VCF)로 증명된 것이지 전체 국면 룩업테이블 존재를 의미하지 않으며, 실제 상태공간이 천문학적이라 전수 저장은 물리적으로 불가능. 오프라인 실행 가능 방식 4종(미니맥스+알파베타+휴리스틱, MCTS 단독, AlphaZero류 사전학습 신경망+로컬추론, 전수 룩업테이블) 비교 후 1순위로 "미니맥스+알파베타+휴리스틱 평가함수+위협공간탐색(VCT/VCF)"를, 2순위(스트레치)로 "사전학습 완료 AlphaZero류 신경망을 로컬 추론 전용으로 내장"을 권고. 핵심 결론: 학습은 있다면 개발 단계에서 사전에 끝내고 노트북에는 추론만 배포하거나, 애초에 학습이 필요 없는 규칙 기반 탐색 알고리즘을 채택해야 함 — "실시간 자체 학습" 방식은 비현실적. |
| 2026-07-10 | 2단계(RL) 실현가능성 조사 + 1단계(Rapfi) 빌드 착수 시도 | **2단계 RL 조사 결론**: dhbloo/rapfi는 Trainer 서브프로젝트(dhbloo/pytorch-nnue-trainer, PyTorch)로 NNUE 학습 코드를 보유하지만 이는 지도학습 파이프라인(외부 가공 데이터셋 필요)이며 self-play 데이터 생성 도구는 미포함 — "완비된 RL 파이프라인 실행"이라는 전제는 틀림. Rapfi/tuning(tuner.cpp/optimizer.cpp)에는 별도 SPSA류 고전 평가함수 파라미터 튜너가 CPU로 존재. GPU 없는 사무용 노트북에서 self-play 데이터 생성부터 NNUE 재학습까지 전면적으로 수행하는 것은 수 주~수개월 이상 소요되며 개선도 보장 못해 **비현실적으로 판단, 기각**. 대안: (1) 기본은 Rapfi 공식 사전학습 Networks 그대로 사용(이미 목표 기력 충족 판단), (2) 굳이 2단계를 진행한다면 "Claude Code가 소량 self-play 기보 생성 + SPSA 튜너로 기존 파라미터 근방 미세 파인튜닝"으로 범위 축소, 전면 재학습은 GPU 확보 전까지 보류. PROJECT_CONTEXT.md에 "2단계 로드맵(RL 고도화)" 섹션으로 기록(착수 안 함, 계획만). **1단계 빌드 착수 결과**: 로컬 Windows 머신에 git(2.53.0)만 설치돼 있고 cmake, MSVC(cl), MinGW(gcc/g++), Visual Studio/Build Tools(vswhere.exe 없음) 전부 미설치 확인 — Rapfi README는 Windows 빌드 시 LLVM/Clang for Windows, Clang-CL for VS, 또는 MinGW-w64-Clang 권장. 빌드 툴체인이 전무해 clone/빌드 시도를 진행하지 않고 chief에 설치 승인 요청으로 보고함(engine/ 디렉터리 미생성, Rapfi 미clone). 렌주 룰은 저장소 소스 확인으로 Rapfi/game/board.h의 `checkForbiddenPoint()`, `RENJU` enum, pattern.h의 `PATTERN2xRenju`로 3-3/4-4/장목 금수 판정이 코드 레벨에 내장돼 있음을 (빌드 없이) 소스 리딩으로 확인. |\n| 2026-07-10 | 요구사항 확정(최상급 기력/렌주룰/15x15/완전 오프라인 스탠드얼론) 후 RCA + 엔진 등급 판단 + 기술스택 최종 결론 | **RCA**: "고급 난이도인데 쉽게 짐" 증상의 통상 원인 = ①고정 얕은 탐색깊이+시간제한 조기종료 ②평가함수가 단순 2·3 패턴만 보고 이중위협(33/44 포크) 못 봄 ③VCT/VCF 강제수 탐색 부재(오목 전술의 핵심은 강제수 시퀀스인데 얕은 미니맥스는 이를 놓침) ④무브 오더링 부실로 알파베타 가지치기 효율 저하 → 같은 시간예산 내 실효 탐색깊이가 낮음 ⑤트랜스포지션 테이블 부재로 중복 계산 낭비 ⑥금수(33/44/장목) 판정 버그로 흑 자충수 방치 또는 오탐 ⑦"고급"이 실제로는 얕은 평가함수의 깊이만 +1~2 올린 것에 불과 — 즉 이름만 바뀌고 엔진 등급 자체가 안 바뀜(사용자가 겪은 것과 정확히 일치하는 패턴). **이번 설계의 회피 방법**: VCT/VCF를 일반탐색과 분리된 필수 레이어로 항상 우선 실행, 반복심화(iterative deepening)+PVS+알파베타, Zobrist 트랜스포지션 테이블, 패턴 기반 강한 무브 오더링, 렌주 금수 판정을 착수 생성 단계에 규칙 정확히 통합(오픈소스 검증된 로직 재사용), 자체 구현 대신 이미 Gomocup 렌주 부문에서 실전 검증된 엔진을 채택해 "약한 평가함수" 리스크 자체를 제거. **엔진 등급 판단**: 15x15 렌주는 아직 완전히 풀리지 않아 "이론상 100% 불패"는 엄밀히 증명 불가(투명성 명시) — 현실적 목표는 "실전에서 인간이 이길 수 없는 수준"(practically superhuman). 이 수준은 아마추어 자체 구현(단순 미니맥스+휴리스틱)으로는 도달 불가함이 사용자의 과거 경험으로 이미 실증됨. Gomocup 렌주 부문 우승급 엔진 기술(VCF/VCT 완전탐색, 강한 정적평가 또는 경량 NN, 반복심화, TT)이 필요하다고 결론. **오픈소스 채택 결정**: dhbloo/rapfi (GitHub, C++, "one of the strongest Gomoku/Renju playing engine", GPL-3.0, 2026-07-09 기준 활발히 유지보수 중, 240 stars) 확인 완료 — Gomocup급 렌주 전용 엔진으로 VCF/VCT/PVS/TT/렌주 금수판정을 이미 구현. 자체 재구현보다 이 엔진을 채택해 리스크 제거. GPL-3.0은 배포 시 소스 동봉 의무가 있으나 본 프로젝트는 사용자 개인 오프라인 단독 설치용이라 준수 부담 없음(설치 패키지에 소스 포함). **최종 기술스택**: 엔진=Rapfi(C++ 네이티브 빌드, Windows x64, Piskvork/pbrain 프로토콜, Renju 룰 플래그 활성화) + GUI=기존 Piskvork 프로토콜 호환 GUI(예: Yixin Board) 우선 채택, 추후 한국어 커스텀 UI 필요시 Python(PyQt)+서브프로세스 통신으로 대체 검토 + 패키징=엔진.exe+GUI.exe+설정파일을 폴더째 zip, 설치 프로그램 없이 xcopy 배포, GPU 불필요(사무용 CPU로 충분), 완전 오프라인 동작. Electron은 불필요한 무게(Chromium 번들)로 기각. |

| 2026-07-10 | 빌드 툴체인 설치 승인 후 MSYS2/MinGW-w64 설치 + Rapfi 빌드/실행 검증 | **설치**: winget 미설치 확인 → MSYS2 공식 nightly 릴리스의 `msys2-base-x86_64-latest.sfx.exe`(7z SFX, GitHub API로 최신 릴리스 조회)를 직접 다운로드해 `C:\msys64`에 무인 압축해제(`x -y -oC:/`) 후 최초 1회 bash 실행으로 초기화 완료. `pacman -Syu` 후 `pacman -S mingw-w64-x86_64-toolchain mingw-w64-x86_64-cmake`로 g++ 16.1.0, cmake 4.3.4, mingw32-make(GNU Make 4.4.1) 설치 확인(gnutls 1개 패키지 미러 404 있었으나 재해결되어 전체 설치 완료, 최종 `g++ --version`/`cmake --version` 정상 출력 확인). **핵심 이슈 발견 및 해결**: 프로젝트 경로 `C:\Users\dntmd\OneDrive\바탕 화면\...`의 "바탕 화면"(한글, Desktop) 세그먼트로 인해 CMake의 try_compile 스크래치 컴파일 및 mingw32-make의 의존성 파일 stat()이 전부 실패(`cc1plus.exe: fatal error: ...: Invalid argument`, `No rule to make target ... zip.c`)함을 발견 — 원인은 mingw-w64 네이티브 Win32 바이너리(cmake.exe, mingw32-make.exe, cc1plus.exe)가 명령행/경로 문자열을 시스템 ANSI 코드페이지 기준으로 처리하면서 UTF-8 바이트로 인코딩된 한글 경로가 손상(mojibake)되어 실제 파일을 찾지 못하는 현상으로 확인(재현 테스트로 검증: ASCII 경로에서는 100% 정상, 한글 경로 개입 시 CMake 설정 단계의 SIMD 자동감지 전부 OFF로 오탐지되거나 make가 소스 파일을 못 찾음). 시스템 로케일/코드페이지 전역 설정 변경(재부팅 필요, 승인 범위 밖)은 하지 않고, 대신 **Rapfi 소스를 ASCII 전용 경로(`C:\rapfi_build\Rapfi`)로 복사해 그 경로에서만 cmake 설정/빌드를 수행**하는 방식으로 우회(원본 clone은 지시대로 `omok/engine/rapfi`에 그대로 보존, git 히스토리 포함 약 1GB — Networks 서브모듈 자체 .git 이력이 958MB로 대부분 차지). **빌드 성공**: `cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release` 후 `cmake --build .`로 `pbrain-rapfi.exe` 링크 완료(경고만 있고 에러 없음; SIMD 자동감지 SSE/AVX2/BMI2 ON 확인). **실행 검증**: Networks 서브모듈(`git submodule update --init Networks`)에서 받은 config.toml + mix9svq NNUE 가중치(렌주 전용 black/white 포함) + classical model210901.bin + mingw64 런타임 DLL 3종(libgcc_s_seh-1, libwinpthread-1, libstdc++-6)을 `omok/engine/dist/`에 모아 실행파일과 함께 배치, Piskvork 프로토콜 stdin으로 `START 15` → `INFO RULE 4`(주의: 단독 `RULE 4`가 아니라 `INFO RULE 4`가 정식 커맨드임을 소스(command/gomocup.cpp의 getOption())로 확인·검증) → `BEGIN` 전송 결과 "MESSAGE Evaluator set to mix9svq." → "mix9svq nnue: load weight from mix9svqrenju_bs15_black.bin.lz4/...white.bin.lz4"(렌주 전용 가중치가 정확히 로드됨, 89ms) → 첫 수 "7,7"(15x15 중앙) 정상 응답까지 확인. `omok/engine/dist/` 폴더만으로(MSYS2 PATH 의존 없이) 독립 실행 성공 — xcopy 배포 가능한 최소 단위 확보. |

## 현재 적용 중인 판단 기준
- 완전 오프라인 동작이 최우선 제약(인터넷/클라우드 API 불가) — 모든 기술 결정에서 최우선 검토 항목.
- 학습(training)과 추론(inference)을 분리해서 사고: 신경망 기반 접근을 택하더라도 학습은 개발 단계(온라인/GPU 환경)에서 끝내고 배포본에는 가중치+추론 코드만 포함.

## chief와 협의한 사항
- 없음 (이번 작업은 기술 검토 및 권고 — 실제 구현 착수 전 chief/사용자의 스택·목표 기력 확정 대기 중)

## 다음 작업 예상
- **패키징**: `omok/engine/dist/`(pbrain-rapfi.exe + config.toml + NNUE/classical 가중치 + mingw64 런타임 DLL 3종, 총 45MB)를 그대로 GUI와 묶어 xcopy 배포 패키지로 완성. GPL-3.0 의무이행을 위해 Rapfi 소스(또는 소스 링크)와 Copying.txt 동봉 필요.
- **GUI 연동(완료, 2026-07-11)**: dhbloo/piskvork_renju GUI 확보·설정 완료. 아래 이력 참고.
- **금수 판정 실전 확인(사용자 직접 필요, 미완료)**: 프로토콜 레벨(`INFO rule 4` 전송 + 렌주 전용 NNUE 가중치 로드)은 검증됐으나, 3-3/4-4/장목 금수를 실제 마우스 클릭으로 재현해 GUI가 이를 거부/회피하는지는 사용자가 직접 확인 필요(체크리스트는 chief 보고에 포함).
- **배포 이식성 참고(해결됨)**: 배포 대상 노트북(삼성 갤럭시 북5 프로, Intel Lunar Lake Core Ultra 5/7 200V)은 AVX2/BMI2/FMA는 지원하나 AVX-512는 미지원 — `-DUSE_AVX2=ON -DUSE_BMI2=ON -DUSE_AVX512=OFF -DUSE_VNNI=OFF` 명시 옵션으로 재빌드해 zmm/avx512 명령어 0건 확인 완료, `omok/engine/dist/pbrain-rapfi.exe` 교체 반영됨. 단, 실제 대상 노트북에서의 최종 실행 테스트는 아직 미실시 — GUI 연동 단계나 별도 인수인계 시 실기 검증 권장.
- 2단계(RL)는 1단계 완료 및 사용자 재승인 시 "소량 self-play + SPSA 파인튜닝"으로 축소된 범위로 재검토(전면 NNUE 재학습은 GPU 확보 전까지 보류).

| 2026-07-10 | 배포 대상 노트북(Lunar Lake, Core Ultra 200V, AVX-512 미지원) 대비 -march=native 재빌드 안전화 | **CPU 확인**: 이 개발 PC의 실제 CPU는 `AMD Ryzen 5 5600 6-Core`(Zen 3) — Zen 3는 AMD가 AVX-512를 도입하기 전(Zen 4부터 지원) 세대라 애초에 AVX-512를 지원하지 않음. 기존 배포본(`omok/engine/dist/pbrain-rapfi.exe`, 이전 -march=native 자동감지 빌드)을 objdump로 재검사한 결과 zmm/avx512 명령어 0건으로, 사실 기존 빌드도 우연히 AVX-512는 안 들어가 있었음(다만 이는 이 개발 PC가 AVX-512 미지원이라 자동감지가 알아서 OFF했을 뿐이지 "명시적으로 안전하게 고정"된 것은 아니었음 — 다른 개발 PC에서 재빌드 시 재발 위험 있었음). **재빌드 방식**: Rapfi CMakeLists.txt 확인 결과 `-march=native`는 SIMD 자동감지(try_compile)용 플래그일 뿐이고, 실제 컴파일 플래그는 `USE_SSE/USE_AVX2/USE_BMI2/USE_AVX512/USE_VNNI` CMake 옵션에 따라 `-mavx2 -mfma -mbmi2` 등으로 명시 지정됨을 확인 — 자동감지에 의존하지 않도록 이 옵션들을 **직접 명시**해 재구성: `cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release -DUSE_SSE=ON -DUSE_AVX2=ON -DUSE_BMI2=ON -DUSE_AVX512=OFF -DUSE_VNNI=OFF`. Lunar Lake(Core Ultra 200V)는 AVX2/FMA/BMI1/BMI2/AES-NI는 지원하지만 Intel이 Alder Lake 이후 클라이언트 제품에서 AVX-512를 제외했으므로 AVX-512/VNNI는 확실히 미지원 — 이 조합이 안전한 타겟. **빌드 환경 이슈 추가 발견**: 이번 Bash 툴 세션은 Git Bash라 PATH에 실제 MSYS2 mingw64(`C:\msys64\mingw64\bin`)가 없어 `cc1.exe: error while loading shared libraries: libmpfr-6.dll`로 컴파일러 자체가 죽는 문제 발생 — `export PATH="/c/msys64/mingw64/bin:$PATH"`로 해결(향후 재빌드 시 이 PATH 설정 필수, MSYS2 전용 셸이 아니면 기본 PATH에 안 잡혀 있을 수 있음). ASCII 경로(`C:\rapfi_build\Rapfi\build\mingw64-safe`)에서 정상 빌드 완료(경고만 있고 에러 없음). **검증 결과**: `objdump -d pbrain-rapfi.exe | grep -icE "zmm|avx512"` → 0건(완전 미포함 확인), `ymm`(AVX2) 19815건·`pext/pdep`(BMI2) 252건으로 의도한 명령어셋만 포함됨을 확인. 버전 문자열도 `"SSE41 AVX2 BMI2"`로 AVX512 표기 없음 재확인. **실행 테스트**: `omok/engine/dist/`에서 직접 `ABOUT`→`START 15`→`INFO RULE 4`→`BEGIN` 전송 결과 mix9svq 렌주 전용 가중치(black/white) 정상 로드, 중앙 착수 `7,7` 정상 응답 확인 — 이전과 동일한 동작 재현. **배포 갱신**: 새 빌드를 `omok/engine/dist/pbrain-rapfi.exe`로 교체(기존 파일 대체), 이전 native 빌드본은 배포 폴더 밖 스크래치 경로에 백업 보관(체크섬으로 신구 바이너리가 다름을 확인, 우연히 파일 크기만 유사). config.toml/NNUE 가중치/DLL 3종은 변경 없음. |

| 2026-07-11 | GUI(Piskvork/pbrain 호환) 확보·연동·레지스트리 사전설정 + 프로토콜 레벨 렌주룰 검증 | **GUI 선택**: Yixin-Board(GitHub accreator/Yixin-Board)는 소스만 있고(GTK+ 빌드 필요) Windows용 프리빌드 exe를 못 찾음 — 대신 원조 Piskvork의 렌주 지원판인 **dhbloo/piskvork_renju**(Rapfi 저자 dhbloo 본인이 만든 포크, `Release/piskvork_renju.zip`, 2020-04-26 빌드)를 채택. 같은 wind23/piskvork_renju(2016년 빌드)도 있으나 dhbloo판이 더 최신이고 Rapfi 저자 신뢰도가 높아 우선 채택. 원조 SourceForge Piskvork(159KB)와 합칠 필요 없이 dhbloo zip 자체가 이미 완결된 단독 실행 패키지(piskvork.exe+skins+language+샘플엔진 pbrain-pela.exe 포함). **배치**: 소스는 `omok/gui/piskvork/`에 보존, 실행/테스트는 한글 경로("바탕 화면") 우회를 위해 ASCII 경로 `C:\Users\dntmd\omok_pkg\`(engine/ + gui/)에 엔진·GUI를 함께 복사해 실사용 배포판 겸 테스트 위치로 구성(향후 xcopy 배포 시 이 폴더를 기준으로 zip 가능). **자동 설정(레지스트리 사전 시딩)**: piskvork_renju 소스(PISKVORK.cpp) 분석으로 설정 저장 위치가 `HKCU\Software\Petr Lastovicka\piskvorky`이고 필드명이 `sirka`(폭)/`height`(높이)/`ruleFive`(0=5목이상승/1=정확히5목/**2=렌주**)/`hraci0`(플레이어0=컴퓨터 여부)/`AI0`(플레이어0 엔진 exe 경로)임을 확인 — PowerShell로 직접 `sirka=15, height=15, ruleFive=2, hraci0=1, AI0=C:\Users\dntmd\omok_pkg\engine\pbrain-rapfi.exe`를 사전 기록해, 사용자가 아무 설정 없이 실행만 해도 15x15/렌주룰/엔진탑재 상태로 뜨도록 만듦(정상적인 `WM_CLOSE`로 종료해야 레지스트리에 저장되는 구조라 `Process.CloseMainWindow()`로 정상 종료 처리). **검증(헤드리스)**: (1) 실행 후 화면 스크린샷으로 GUI가 "rapfi 5.0"을 플레이어0 이름으로 정상 인식·표시함을 시각 확인(엔진 ABOUT 응답 반영), 보드가 15x15로 그려짐 확인. (2) `logPipe=1` 레지스트리로 파이프 로그를 활성화해 재실행 → `omok_pkg/engine/pbrain-rapfi.log`에서 `START 15` → `INFO rule 4`(렌주룰 명령 실제 전송 확인) → `mix9svqrenju_bs15_black.bin.lz4`/`..._white.bin.lz4` 로드 → 실제 탐색(Depth 18-38, Node 1148K) 후 정상 착수 응답까지 프로토콜 레벨로 완전 검증. 검증 후 `logPipe`는 도로 0으로 되돌려 기본 상태를 깔끔하게 유지. **렌주 금수 GUI 자체 판정 기능 확인**: dhbloo/piskvork_renju 소스에 `checkforbid.cpp`(forbid()/checkforbid() 함수)가 별도로 존재 — 이는 GUI 자체가 클릭 시점에 렌주 금수(3-3/4-4/장목)를 검사하는 클라이언트 사이드 로직으로, 엔진 응답과 별개로 사람이 흑으로 직접 금수를 클릭 시도해도 GUI 차원에서 검증 가능한 구조임을 소스 레벨로 확인(단, 실제 클릭 시나리오까지는 자동화하지 않음 — 좌표 계산+마우스 시뮬레이션은 legacy 커스텀 드로잉 컨트롤이라 시간 대비 리스크 높다고 판단, 사용자 직접 테스트로 위임). **메뉴 위치 확인(소스의 .rc 리소스 확인)**: 렌주룰 라디오버튼은 `Game > Options...` 다이얼로그 안에 "renju rule"이라는 정확한 라벨로 존재(폭/높이 입력란도 같은 다이얼로그). 플레이어 교체는 `Players > Swap players`, 엔진 재지정은 플레이어 이름 더블클릭 → Open AI 다이얼로그. |

## 참고: 향후 재빌드 시 필독 (한글 경로 이슈 + PATH 이슈 + 안전 SIMD 옵션)
- 이 프로젝트 전체가 `C:\Users\dntmd\OneDrive\바탕 화면\...`(한글 "바탕 화면" 포함) 아래에 있어, MinGW 네이티브 툴체인(cmake/make/gcc)으로 이 경로에서 직접 빌드하면 ANSI 코드페이지 손상으로 실패한다. 재빌드/업데이트 시 반드시 소스를 ASCII 전용 경로(예: `C:\rapfi_build`)로 복사한 뒤 그곳에서 cmake 설정+빌드를 수행하고, 결과물만 `omok/engine/dist/`로 복사할 것. (일반 파일 복사/git/Read/Write 등은 이 경로에서 문제없이 동작 — 오직 MinGW 네이티브 빌드 도구 체인만 영향받음)
- Bash 툴(Git Bash) 세션은 기본 PATH에 실제 MSYS2 mingw64 bin이 안 잡혀 있어 `cc1.exe: error while loading shared libraries: libmpfr-6.dll` 로 컴파일 자체가 실패한다. 빌드 전 반드시 `export PATH="/c/msys64/mingw64/bin:$PATH"` 실행할 것.
- **CPU 호환성**: 실제 배포 대상(삼성 갤럭시 북5 프로, Lunar Lake)은 AVX2/BMI2/FMA는 지원, AVX-512는 미지원. cmake 설정 시 자동감지(`-march=native` 기반 try_compile)에 맡기지 말고 항상 `-DUSE_SSE=ON -DUSE_AVX2=ON -DUSE_BMI2=ON -DUSE_AVX512=OFF -DUSE_VNNI=OFF`를 명시적으로 지정할 것 — 이렇게 하면 빌드를 수행하는 개발 PC의 CPU 종류(AVX-512 지원 여부)와 무관하게 항상 동일하게 안전한 바이너리가 나온다. 재빌드 후 `objdump -d pbrain-rapfi.exe | grep -icE "zmm|avx512"` 결과가 반드시 0이어야 한다.
